name: GradientBoostingTest

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators: { type: float, default: 100.0 }
      learning_rate: { type: float, default: 1.0 }
      max_depth: { type: float, default: 5.0 }
    command: "python train.py -r {n_estimators} {learning_rate} {max_depth}"
  # validate:
  # parameters:
  #   data_file: path
  # command: "python validate.py {data_file}"
